import React from 'react';
import Layout from '../layout/Layout';
import Menu from '../../menu/Menu';

const IdentificationcardRequest = () => {
   return (
      <Layout>
         <Menu />
      </Layout>
   );
};

export default IdentificationcardRequest;
